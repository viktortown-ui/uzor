import type { WrappedReport } from '../wrappedTypes';
import { WrappedCard } from './WrappedCard';
export function WrappedHero({ report }: { report: WrappedReport }) { const items = [['Главная тема недели', report.mainTheme.label, `${report.mainTheme.share}%`, report.mainTheme.description], ['Подтверждения', `${report.summary.confirmedSignals} ваших сигнала подтвердились`, '', 'Круг подтвердил эти наблюдения.'], ['Точность сигналов', `${report.summary.accuracy}%`, '', 'Ваши сигналы совпали с независимыми откликами круга.'], ['Раньше других', `${report.summary.earlySignals} раз`, '', 'Вы увидели ветку до усиления.'], ['Ваш стиль', report.identity.style, '', 'Вы взвешиваете риски и подтверждаете факты.']]; return <div className="wrapped-hero-grid"><WrappedCard className="wrapped-hero-card"><div className="wrapped-orb">✦</div><p>Ваш итог недели</p><h2>{report.identity.title}</h2><span>{report.identity.subtitle}</span>{report.identity.percentileText && <b>{report.identity.percentileText}</b>}</WrappedCard>{items.map(([k, title, num, text], i) => <WrappedCard delay={(i + 1) * .04} key={k}><p>{k}</p><h3>{title}</h3>{num && <strong>{num}</strong>}<span>{text}</span></WrappedCard>)}</div>; }
