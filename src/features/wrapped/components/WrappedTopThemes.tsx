import type { WrappedTheme } from '../wrappedTypes';
export function WrappedTopThemes({ themes }: { themes: WrappedTheme[] }) { const rows = themes.filter((theme) => theme.share > 0).slice(0,3); if (!rows.length) return <div className="wrapped-empty-mini"><b>Пока мало данных по темам</b><span>Добавьте несколько сигналов — здесь появятся три направления, которые вы чаще всего замечали.</span></div>; return <div className="wrapped-theme-list">{rows.map((theme, i) => <div className="wrapped-bar" key={theme.label}><em>{i + 1}</em><span>{theme.label}</span><b>{theme.share}%</b><i><u style={{ width: `${theme.share}%` }}/></i></div>)}</div>; }
