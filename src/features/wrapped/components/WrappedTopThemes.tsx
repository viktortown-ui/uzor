import type { WrappedTheme } from '../wrappedTypes';
export function WrappedTopThemes({ themes }: { themes: WrappedTheme[] }) { const rows = themes.slice(0,3); if (!rows.length) return <div className="wrapped-empty-mini"><b>Темы ещё собираются</b><span>Добавьте сигналы, и здесь появится рейтинг наблюдений.</span></div>; return <div className="wrapped-theme-list">{rows.map((theme, i) => <div className="wrapped-bar" key={theme.label}><em>{i + 1}</em><span>{theme.label}</span><b>{theme.share}%</b><i><u style={{ width: `${theme.share}%` }}/></i></div>)}<button type="button" className="wrapped-soft-action">Смотреть все темы ›</button></div>; }
