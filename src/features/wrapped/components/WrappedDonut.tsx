export function WrappedDonut({ value, confirmed = 0, total = 0 }: { value: number; confirmed?: number; total?: number }) { const radius = 45; const length = 2 * Math.PI * radius; const verdict = value >= 60 ? 'Отличный результат!' : value === 0 ? 'Пока мало данных — контур честно ждёт подтверждений' : 'Контур набирает силу'; return <div className="wrapped-donut-wrap"><svg className="wrapped-donut" viewBox="0 0 120 120" role="img" aria-label={`Точность ${value}%`}><defs><linearGradient id="donutX" x1="0" x2="1"><stop stopColor="#25ffb8"/><stop offset="1" stopColor="#20d7ff"/></linearGradient></defs><circle cx="60" cy="60" r={radius} className="donut-halo"/><circle cx="60" cy="60" r={radius} className="donut-bg"/><circle cx="60" cy="60" r={radius} className="donut-fg" strokeDasharray={length} strokeDashoffset={length - (length * value) / 100}/><text x="60" y="66" textAnchor="middle">{value}%</text></svg>{value > 0 && <em className="donut-delta">+12%</em>}<strong>{verdict}</strong><span>{confirmed} из {total} сигналов получили независимый отклик.</span></div>; }
