import type { WrappedReport } from '../wrappedTypes';
import { WrappedCard } from './WrappedCard';
export function WrappedMetricCards({ report }: { report: WrappedReport }) { const metrics = [['⌁','Сигналов за неделю', report.summary.signalsThisWeek, report.summary.previousWeekDelta != null ? `${report.summary.previousWeekDelta >= 0 ? '+' : ''}${report.summary.previousWeekDelta} vs прошл. неделя` : 'пока без сравнения'], ['⬟','Подтверждено', report.summary.confirmedSignals, report.summary.confirmedSignals ? 'подтверждено кругом' : 'ожидаем отклики'], ['◎','Точность', `${report.summary.accuracy}%`, report.summary.trendDelta != null ? `${report.summary.trendDelta >= 0 ? '+' : ''}${report.summary.trendDelta}%` : 'пока без сравнения'], ['ϟ','Раньше круга', report.summary.earlySignals, report.summary.earlySignals ? 'ранний сигнал' : 'без ранних совпадений'], ['♨','Серия недель', report.summary.weekStreak, report.summary.weekStreak ? 'держите ритм' : 'серия начнётся']]; return <div className="wrapped-metrics">{metrics.map(([icon, k, v, change]) => <WrappedCard className="wrapped-metric" key={k as string}><i>{icon}</i><div><p>{k}</p><h3>{v}</h3></div><small>{change}</small></WrappedCard>)}</div>; }
