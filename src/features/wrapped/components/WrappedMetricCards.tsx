import type { WrappedReport } from '../wrappedTypes';
import { WrappedCard } from './WrappedCard';
export function WrappedMetricCards({ report }: { report: WrappedReport }) { const metrics = [['pulse','Сигналов за неделю', report.summary.signalsThisWeek, report.summary.previousWeekDelta != null ? `${report.summary.previousWeekDelta >= 0 ? '+' : ''}${report.summary.previousWeekDelta} vs прошл. неделя` : 'ваш вклад'], ['shield','Подтверждено', report.summary.confirmedSignals, report.summary.confirmedSignals ? 'подтверждено кругом' : 'ждём независимый отклик'], ['target','Точность', `${report.summary.accuracy}%`, report.summary.signalsThisWeek ? 'по подтверждённым' : 'данных мало'], ['flame','Серия недель', report.summary.weekStreak, report.summary.weekStreak ? 'держите ритм' : 'серия начнётся']]; return <div className="wrapped-metrics wrapped-metrics-mvp">{metrics.map(([icon, k, v, change]) => <WrappedCard className={`wrapped-metric metric-${icon}`} key={k as string}><i aria-hidden="true"><svg viewBox="0 0 42 42"><path d="M5 22h8l4-12 8 24 5-12h7"/><circle cx="21" cy="21" r="15"/></svg></i><div><p>{k}</p><h3>{v}</h3></div><small>{change}</small></WrappedCard>)}</div>; }
