import { Link } from 'react-router-dom';
import type { WrappedRightSignal } from '../wrappedTypes';
export function WrappedRightSignals({ signals }: { signals: WrappedRightSignal[] }) { if (!signals.length) return <div className="wrapped-right-empty"><div>✓</div><h3>Пока нет подтверждённых сигналов</h3><p>Ваши сигналы уже сохранены. Нужно, чтобы другой участник независимо отметил похожую ветку.</p><Link to="/contribute">Добавить сигнал</Link></div>; return <div className="wrapped-signal-list">{signals.slice(0,3).map((signal) => <article key={`${signal.title}-${signal.time}`}><i>✓</i><div><strong>{signal.title}</strong><span>{signal.tag}</span>{signal.consequence && <p>{signal.consequence}</p>}</div><small>{signal.status}<br/><em>{signal.time}</em></small></article>)}</div>; }
