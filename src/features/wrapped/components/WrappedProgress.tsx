import type { WrappedReport } from '../wrappedTypes';
export function WrappedProgress({ report }: { report: WrappedReport }) { const xpPercent = Math.min(100, Math.round((report.summary.xp / Math.max(report.summary.nextLevelXp, 1)) * 100)); return <div className="wrapped-progress-layout"><div className="wrapped-levels"><span><i>✧</i>{report.progress.previousLevel}<small>Получено</small></span><strong><i>✦</i>{report.progress.currentLevel}<small>Текущий уровень</small></strong><span className="locked"><i>{report.progress.nextLevelLocked ? '🔒' : '✧'}</i>{report.progress.nextLevel}<small>До следующего: {report.summary.xpToNextLevel} XP</small></span></div><div className="wrapped-xp-panel"><p>До следующего уровня</p><h3>{report.summary.xpToNextLevel.toLocaleString('ru-RU')} <span>XP</span></h3><div className="wrapped-xp"><i style={{ width: `${xpPercent}%` }}/></div><small>{report.summary.xp.toLocaleString('ru-RU')} из {report.summary.nextLevelXp.toLocaleString('ru-RU')} XP</small><em>Продолжайте замечать сдвиги раньше других.</em></div></div>; }
