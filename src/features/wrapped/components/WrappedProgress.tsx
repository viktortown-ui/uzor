import type { WrappedReport } from '../wrappedTypes';
export function WrappedProgress({ report }: { report: WrappedReport }) { const xpPercent = Math.min(100, Math.round((report.summary.xp / Math.max(report.summary.nextLevelXp, 1)) * 100)); return <div className="wrapped-progress-layout"><div className="wrapped-levels"><strong><i>✦</i>{report.progress.currentLevel}<small>Текущий уровень</small></strong><span className="locked"><i>{report.progress.nextLevelLocked ? '⌽' : '✧'}</i>{report.progress.nextLevel}<small>До следующего: {report.summary.xpToNextLevel} XP</small></span></div><div className="wrapped-xp-panel"><p>XP прогресс</p><h3>{report.summary.xp.toLocaleString('ru-RU')} <span>/ {report.summary.nextLevelXp.toLocaleString('ru-RU')}</span></h3><div className="wrapped-xp"><i style={{ width: `${xpPercent}%` }}/></div><small>До следующего уровня: {report.summary.xpToNextLevel.toLocaleString('ru-RU')} XP</small><em>Продолжайте замечать сдвиги — круг подтвердит сильные сигналы.</em></div></div>; }
