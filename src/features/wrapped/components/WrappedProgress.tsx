import type { WrappedReport } from '../wrappedTypes';
export function WrappedProgress({ report }: { report: WrappedReport }) { const xpPercent = Math.min(100, Math.round((report.summary.xp / Math.max(report.summary.nextLevelXp, 1)) * 100)); return <><div className="wrapped-levels"><span>{report.progress.previousLevel}</span><strong>{report.progress.currentLevel}</strong><span>{report.progress.nextLevelLocked ? '🔒 ' : ''}{report.progress.nextLevel}</span></div><div className="wrapped-xp"><i style={{ width: `${xpPercent}%` }}/></div><p>{report.summary.xp} XP · до следующего уровня {report.summary.xpToNextLevel} XP</p><small>Продолжайте замечать сдвиги раньше других.</small></>; }
