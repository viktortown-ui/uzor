import { buildDeltaStatement, getChangeTypeOptions, getImpactOptions, getSubjectPlaceholder } from '../deltaCreateLogic';
import type { DeltaCreateDraft } from '../deltaCreateTypes';
import type { DeltaCategory } from '../../deltas/deltaTypes';
const periods = [{value:'today',label:'Сегодня'},{value:'last_3_days',label:'Последние 3 дня'},{value:'last_week',label:'Неделя'},{value:'last_2_4_weeks',label:'2–4 недели'}] as const;
function Chip({active,children,onClick}:{active:boolean;children:React.ReactNode;onClick:()=>void}){return <button type="button" className={active?'mobile-delta-chip active':'mobile-delta-chip'} aria-pressed={active} onClick={onClick}>{children}</button>}
export function MobileDeltaChangeScreen({draft,update,categories,catStatus,retry,errors,headingRef}:{draft:DeltaCreateDraft;update:(p:Partial<DeltaCreateDraft>,c?:string)=>void;categories:DeltaCategory[];catStatus:string;retry:()=>void;errors:string[];headingRef?: React.RefObject<HTMLHeadingElement | null>}){const preview=draft.statementMode==='auto'?buildDeltaStatement(draft):draft.statement;return <section className="mobile-delta-screen"><h1 ref={headingRef} tabIndex={-1}>Что изменилось?</h1><p>Опишите одно заметное изменение рядом с вами.</p>{errors.length>0&&<div role="alert" className="mobile-delta-alert">{errors.map(e=><p key={e}>{e}</p>)}</div>}<fieldset><legend>Направление</legend><div className="mobile-delta-segment"><Chip active={draft.direction==='negative'} onClick={()=>update({direction:'negative'},'direction')}>Стало хуже</Chip><Chip active={draft.direction==='positive'} onClick={()=>update({direction:'positive'},'direction')}>Стало лучше</Chip></div></fieldset><fieldset><legend>Где проявилось изменение?</legend>{catStatus==='loading'&&<p role="status">Загружаем категории…</p>}{catStatus==='error'&&<p role="alert">Не удалось загрузить категории <button type="button" onClick={retry}>Повторить</button></p>}{catStatus==='empty'&&<p role="alert">Категории Дельт пока не настроены <button type="button" onClick={retry}>Повторить</button></p>}{catStatus==='ready'&&<div className="mobile-delta-tiles">{categories.map(c=><Chip key={c.slug} active={draft.categorySlug===c.slug} onClick={()=>update({categorySlug:c.slug as DeltaCreateDraft['categorySlug']},'categorySlug')}>{c.title}</Chip>)}</div>}</fieldset>{draft.direction&&<fieldset><legend>Как именно изменилось?</legend><div className="mobile-delta-tiles">{getChangeTypeOptions(draft.direction).map(o=><Chip key={o.value} active={draft.changeType===o.value} onClick={()=>update({changeType:o.value},'changeType')}>{o.label}</Chip>)}</div></fieldset>}<label>Коротко опишите изменение<input value={draft.subject} maxLength={80} onChange={e=>update({subject:e.target.value},'subject')} placeholder={getSubjectPlaceholder(draft.categorySlug)} /></label>{preview&&<article className="mobile-delta-preview"><span>Ваша Дельта</span>{draft.statementMode==='manual'?<textarea value={draft.statement} maxLength={180} onChange={e=>update({statement:e.target.value},'statement')} aria-label="Формулировка Дельты"/>:<strong>{preview}</strong>}<button type="button" onClick={()=>update({statementMode:draft.statementMode==='manual'?'auto':'manual',statement:preview},'statement')}>{draft.statementMode==='manual'?'Вернуть автоматическую формулировку':'Уточнить формулировку'}</button></article>}<fieldset><legend>Когда заметили?</legend><div className="mobile-delta-tiles">{periods.map(o=><Chip key={o.value} active={draft.observedWindow===o.value} onClick={()=>update({observedWindow:o.value})}>{o.label}</Chip>)}</div></fieldset><fieldset><legend>Насколько влияет?</legend><div className="mobile-delta-tiles">{getImpactOptions(draft.direction).map(o=><Chip key={o.value} active={draft.impactLevel===o.value} onClick={()=>update({impactLevel:o.value})}>{o.label}</Chip>)}</div></fieldset><details><summary>+ Добавить подробность</summary><textarea value={draft.details} maxLength={500} onChange={e=>update({details:e.target.value})} /><p>{draft.details.length}/500</p></details></section>}
