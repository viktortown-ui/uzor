import type{ConsiderationVote,PublicProposal}from'../api/forecastQuestionApiTypes';
export function ForecastProposalCard({proposal,onVote,busy=false}:{proposal:PublicProposal;onVote?:(v:ConsiderationVote)=>void;busy?:boolean}){const selected=proposal.status==='selected';return <article className="future-card"><p className="future-badge">{selected?'ВЫБРАНО ДЛЯ ПОДГОТОВКИ':'РАССМОТРЕНИЕ ТЕМЫ · НЕ ПРОГНОЗ'}</p><h3>{proposal.publicTitle}</h3><p>{proposal.publicSummary}</p>{proposal.locationLabel&&<p>📍 {proposal.locationLabel}</p>}<p><b>{proposal.totalVotes}</b> участников · стоит рассмотреть: {proposal.supportCount} · не сейчас: {proposal.notNowCount}</p>{selected?<p>Редактор готовит проверяемую формулировку, варианты исхода, срок и источник проверки.</p>:<><p>Это выбор темы для подготовки, а не прогноз события.</p><div className="future-votes"><button disabled={busy} aria-pressed={proposal.viewerVote==='support'} onClick={()=>onVote?.('support')}>Да, стоит рассмотреть</button><button disabled={busy} aria-pressed={proposal.viewerVote==='not_now'} onClick={()=>onVote?.('not_now')}>Нет, не сейчас</button></div><small>Результат помогает редактору определить приоритет, но не публикует прогноз автоматически.</small></>}</article>}
