import { useState } from 'react';
import type { LabV4Data } from './LabV4Types';
export function WhyScene({ data }: { data: LabV4Data }) { const [more, setMore] = useState(false); return <section className="v4-why"><h2>Почему {data.mainPath.source.toLowerCase()} стал главным сигналом?</h2><p className="v4-chain">{data.mainPath.source} → {data.mainPath.change.toLowerCase()} → {data.mainPath.consequence.toLowerCase()}</p>{data.forkPath && <p className="v4-chain fork">{data.forkPath.source} → {data.forkPath.change.toLowerCase()} → {data.forkPath.consequence.toLowerCase()}</p>}<p>{data.mainPath.participants} независимых откликов · {data.mainPath.contexts} жизненных контекста</p><p>Эта связь повторяется не у одного человека, а в нескольких разных повседневных ситуациях.</p><button onClick={() => setMore(!more)}>Почему мы это показываем?</button>{more && <div className="v4-trust-list">{data.trustFacts.map((f) => <p key={f}>{f}</p>)}<button>Посмотреть другие пути</button></div>}</section>; }
