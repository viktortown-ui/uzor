import { useState } from 'react';
import type { LabV4Data } from './LabV4Types';
export function FutureFlow({ data }: { data: LabV4Data }) { const [h, setH] = useState('30 дней'); const values = data.future.horizons[h]; return <section className="v4-future"><p className="v4-eyebrow">{data.future.subject} · ближайшие {h}</p><div className="v4-horizons">{Object.keys(data.future.horizons).map((x) => <button className={h === x ? 'active' : ''} onClick={() => setH(x)} key={x}>{x}</button>)}</div><div className="v4-future-river" aria-label="Ожидания ближайшего времени"><span className="now">Сейчас</span><div className="future-line grow" style={{ '--w': `${values.grow}%` } as React.CSSProperties}><b>{values.grow}%</b> ждут усиления</div><div className="future-line stable" style={{ '--w': `${values.stable}%` } as React.CSSProperties}><b>{values.stable}%</b> без заметных изменений</div><div className="future-line relief" style={{ '--w': `${values.relief}%` } as React.CSSProperties}><b>{values.relief}%</b> ждут облегчения</div><span className="then">Ближайшие {h}</span></div>{values.note && <p className="v4-long-note">{values.note}</p>}</section>; }
