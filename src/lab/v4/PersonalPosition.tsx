import type { LabV4Choice, LabV4Data } from './LabV4Types';
const choices: LabV4Choice[] = ['Продукты', 'Транспорт', 'Услуги', 'Пока не знаю'];
export function PersonalPosition({ data, choice, onChoice }: { data: LabV4Data; choice: LabV4Choice | null; onChoice: (c: LabV4Choice) => void }) { const same = choice === data.mainPath.source; return <section className="v4-personal"><h2>Я среди других</h2><p>Что ты замечаешь сильнее всего?</p><div>{choices.map((c) => <button className={choice === c ? 'active' : ''} key={c} onClick={() => onChoice(c)}>{c}</button>)}</div>{choice && <p className="v4-personal-result">{same ? `Ты ближе к 42% круга: они тоже сильнее всего замечают ${data.mainPath.source.toLowerCase()}.` : 'Ты замечаешь то, что пока видит меньшая часть круга. Это не ошибка. Так могут появляться ранние сигналы.'}</p>}</section>; }
