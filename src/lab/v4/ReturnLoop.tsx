import type { LabV4Data } from './LabV4Types';
export function ReturnLoop({ data, visible }: { data: LabV4Data; visible: boolean }) { if (!visible) return null; return <section className="v4-return"><h2>Твой вклад в ясность</h2><p>Ты: заметил, подтвердил или показал другой путь.</p><p className="v4-progress">Личный след → Есть отклик → Связь повторяется → Картина разделилась → Сдвиг изменился</p><h3>Что изменилось после твоего прошлого входа</h3>{data.returnEvents.slice(0, 2).map((e) => <p key={e}>{e}</p>)}<p>Картина станет яснее, когда эту связь независимо подтвердят ещё 3 человека.</p></section>; }
